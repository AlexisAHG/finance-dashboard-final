import streamlit as st

st.set_page_config(page_title="Single Asset", layout="wide")
st.title("Single-asset backtest (Quant A)")

st.write("BTC/USD momentum / basic backtest. WIP.")

# TODO: call quant-a functions here
# ex: from src.analytics.xxx import run_backtest
# result = run_backtest(...)
# st.line_chart(result["equity_curve"])
